import { Button, FeatureCard } from '../components';
import { platformsitesA } from '../constants';

const Platformsites = () => {
  return (
    <section id="platformsites" className="flex flex-col md:flex-row sm:py-16 py-6">
      <div className="flex justify-center items-start flex-col">
        <h2 className="heading-2 text-white">


 This has the user Platformsites section   <br />
<p className="paragraph text-white">
 gives some basic ainformation on the other sites  salesRep localBiz  links to a sep page with more data information <br />
is this even possible <br />
 thea sep page would have the same navBar <br />
 what why how referral process works <br />

etc..<br />
[-Platform Data -TgtColleges]   (Separate Page)
	tgtColleges
	Areas
	num of local companies
	types of companies

 -allow user to see number of tgt colleges/st/cities
 -also est college population
 -est of number of local businesses/categories
 -as user what biz/type/category of biz users would 
  want to use/see advertising

        </h2>

      </div>

    </section>
  );
};

export default Platformsites;

